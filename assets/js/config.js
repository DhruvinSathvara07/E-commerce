'use strict';

/**
 * Global Configuration
 * Contains API URLs, currency, language, and theme settings
 */
const CONFIG = {
    // API Configuration
    API: {
        URL: 'https://fakestoreapi.com/products'
    },

    // Currency Configuration
    CURRENCY: {
        symbol: '$',
        code: 'USD'
    },

    // Language Configuration
    // Language Configuration
    LANGUAGE: {
        code: 'en',
        name: 'English'
    },

    // Theme Configuration
    THEME: {
        mode: 'light', // 'light' or 'dark'
        primaryColor: '#3b82f6'
    }
};

